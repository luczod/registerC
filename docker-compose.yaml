services:
      app:
            container_name: "registerc-dev"
            build: .
            volumes:
                  - .:/registerC
                  - /tmp/.X11-unix:/tmp/./X11-unix
            environment:
                  - DISPLAY=${DISPLAY}
            ports:
                  - "${PORT}:8080"



